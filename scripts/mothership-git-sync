#!/usr/bin/env bash
# back-compat wrapper → host-git-sync (role=mothership)
set -euo pipefail
here="$(cd "$(dirname "$0")" && pwd)"
export SYNC_ROLE="${SYNC_ROLE:-mothership}"
export SYNC_FLAKE="${SYNC_FLAKE:-${MOTHERSHIP_FLAKE:-mothership}}"
exec bash "$here/host-git-sync" "$@"
